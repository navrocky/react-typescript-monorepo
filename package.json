{
  "name": "react-monorepo-demo",
  "description": "Template for setting up a TypeScript monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
  },
  "devDependencies": {
  }
}
